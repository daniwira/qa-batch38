var x= 36;
var angka=Math.sqrt(x) 
if (x%2==0) 
{
   console.log("harus bilangan genap");
   if (angka > 0)
   {
      console.log("input " + x + " hasil = " + angka);
   }
   else
   {
      console.log("Tidak bisa input bilangan negatif");
   }
}           
else
{
   console.log("harus bilangan genap");
   if (angka > 0)
   {
      console.log("Tidak bisa input bilangan ganjil" + " " +x);
   }
   else
   {
      console.log("Tidak bisa input bilangan negatif");
   }
}